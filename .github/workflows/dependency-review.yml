name: Dependency Review

on:
  pull_request:
    # branches: [main] # TODO enable

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
